# WAM2Repair Experiment Tracker

## 2026-09-18 Strict analytic-depth geometry pilot

- `geometry_target_e4/e14/e23/e24_20260918T101500Z_2300000..3`: `BUILT_CONSERVATIVE_TARGET_ONLY_GEOMETRY_BRIDGE`. For each canonical D0/WAM pair, factual RGB reconstitutes the target byte-exactly under the fixed rotate-180/resizing transform. Depth is target-only: a pixel remains valid only when the source analytic ray was finite and exact-ID-gated. Per-frame native-grid coverage is 0.9006--0.9102. Pair manifest, D0 manifest/candidate, factual target, action offsets `[0,2,4,6,8]`, and output SHA are bound; geometry is never an input to FastWAM or the repair model.
- `real_wam_repair_geometry_val_holdout_episodes4_14_val23_test24_20260918T103500Z_2301001`: `EVALUATED_VAL_SELECTED_EPISODE_HELDOUT_GEOMETRY_PILOT`. Train source episodes are e4/e14, validation e23 selects step 400, and e24 is first opened only after the selected checkpoint is saved. Input is exclusively raw WAM RGB/current RGB/actions/proprio; factual future RGB and analytic depth are supervision/evaluation only. Heldout RGB MAE is `0.09002755 -> 0.04506526`; temporal MAE is `0.04420660 -> 0.03196612`; predicted-depth log MAE is `0.95764583` and AbsRel is `2.79582953` on valid future support `0.87530`. Artifact SHA verification passed for checkpoint `1492df...fed1` and predictions `e44616...d402`; GPU2 was released to 0 MiB/no compute process.
- Interpretation gate: the RGB reduction is a four-episode exploratory reconstruction result only. The high predicted-depth error means this run does **not** demonstrate usable geometry correction. Analytic-depth supervision/error is not certification of penetration, contact, collision, object pose, temporal 4D consistency, WAM improvement, or pi0.5/VLA success; no copy/repeated-current-frame baseline was run.

## 2026-09-18 B0 agentview ray/depth convention diagnosis

- `geometry_ray_replay_20260918T053530Z_2256012`: `COMPLETED_DIAGNOSIS_ONLY` on CPU OSMesa. This does **not** approve B0, D0 data collection, physical labels, WAM repair, or VLA benefit.
- Across all three saved `libero_object` snapshots, every sampled `mj_ray` geometry ID matched the renderer segmentation ID (agentview 362/362; wrist 379--380/380), and K, camera-to-world, saved depth, and segmentation replayed exactly. Cross-process RGB did not replay bit-exactly (agentview 820--824 pixels; wrist 3001--3003 pixels), so that difference remains recorded rather than waived.
- The locked/default pixel-center + axial-depth formulation remains the best tested formulation: agentview p95 depth error is 7.443 mm (threshold 5 mm; FAIL) at every snapshot; wrist p95 is 0.293--0.333 mm (PASS). Pixel-corner axial depth worsens agentview to 17.446 mm; raw ray-distance alternatives worsen it to 225.562--225.819 mm. Therefore this is not a simple pixel-origin or axial-vs-range convention error.
- Next gate: independently localize the systematic agentview raster-depth versus analytic-ray discrepancy, with the original B0 threshold and protocol frozen. Do not convert the diagnostic probes into a post-hoc passing definition and do not begin D0/B1/B2/D2 from this evidence.
- `native_depth_renderer_20260918T060000Z_2259000`: preserved `ERROR` before any renderer comparison because the selected render-only Conda prefix lacks `torch`, which LIBERO imports. No GPU was used and no result is inferred.
- `native_depth_renderer_20260918T060120Z_2260000`: preserved `ERROR` before any renderer comparison because the independently required reconstructed-model XML SHA did not equal the snapshot-era XML SHA. The mismatch is intentionally treated as an isolation failure, not waived as a renderer-path finding. Locate/reproduce the snapshot-era model environment before rerunning this diagnostic.
- `native_depth_snapshot_20260918T060430Z_2263000`: `COMPLETED_DIAGNOSIS_ONLY` using only the recorded, hash-verified snapshot XML plus saved state (no LIBERO reconstruction). The top-down native renderer output is much closer than bottom-up, but is still not bit-exact: agentview p95 absolute depth difference 9.594 mm and wrist 8.377--8.379 mm; bottom-up p95 is 1.9912 m and 0.3111--0.3113 m respectively. This localizes a material stored-depth versus native-renderer output discrepancy, but establishes neither physical ground truth nor a replacement B0 definition. B0 remains blocked.
- `native_depth_ray_20260918T060800Z_2265000`: `COMPLETED_DIAGNOSIS_ONLY`, after recording both native segmentation channel orders and both image orientations. Native renderer uses the reverse segmentation-channel order. With the channel order repaired only for diagnosis, top-down native agentview has ray depth p95 2.325 mm but geometry-ID agreement 0.9393 (<0.95); wrist has p95 6.613 mm and geometry-ID agreement 0.7923. Bottom-up is decisively worse. Since neither camera satisfies all frozen criteria and XML hashes do not fingerprint mutable mesh assets, this is not a B0 pass and cannot select a new renderer/encoding post hoc.
- `asset_provenance_20260918T061000Z_2266000`: `PASS_ASSET_PROVENANCE_ONLY`. All 82 distinct absolute external assets referenced by the hash-verified `libero_object_model.xml` match the snapshot-era geometry-gate manifest; the manifest is bound to the exact atomic source-result SHA and duplicate-conflicting entries are rejected. This eliminates external mesh/texture file drift as an explanation for the native-vs-stored discrepancy, but does not validate a renderer path, B0, D0, physical labels, repair, WAM, or VLA.
- `native_depth_ray_20260918T061300Z_2268000`: `COMPLETED_DIAGNOSIS_ONLY`. With a public `MjvOption.geomgroup=[0,1,1,0,0,0]` matching the frozen `mj_ray` mask, native segmentation (after the documented channel swap) is exactly equal to saved top-down robosuite segmentation at all three snapshots: 0 mismatched elements for both cameras. Under that same path, ray geom-ID agreement is 1.0 and depth p95 is 0.00000511 m (agentview) / 0.000000043--0.000000049 m (wrist). Bottom-up is strongly rejected. This identifies the old `get_real_depth_map` stored-depth path, rather than geometry/assets/camera orientation, as the discrepancy. It is still not a retroactive B0 pass: collect fresh, independently held-out snapshots with robosuite RGB/seg plus native grouped metric depth, prove same-pixel segmentation alignment, then rerun the frozen B0 gate on those new snapshots.
- `atomic_native_depth_20260918T061500Z_2269000`: `PASS_NATIVE_DEPTH_ATOMIC_REPEATABILITY_ONLY` on fresh init-6 snapshots (not legacy init-4). It stores robosuite RGB plus native grouped metric depth and its matching native grouped segmentation; each camera/frame asserts exact equality with the corresponding robosuite top-down segmentation. This is a B0 input prerequisite, not D0/full trajectories or physical/WAM/VLA evidence.
- `geometry_ray_native_depth_20260918T061700Z_2270000`: `PASS_RAY_RASTER_ONLY` under the original unchanged sampling, pixel-center/axial-depth convention, thresholds (p95 <=5 mm), ray geomgroup, and vertical-flip negative control. The explicit source contract requires the fresh native-depth source status, init-6, and exact depth-backend string. All 12 camera/tick checks pass: geom-ID agreement=1.0; p95 depth errors range 0.000000054--0.000000438 m. This opens D0 factual-bank collection only; it does not establish collision/SDF labels, WAM repair, or VLA improvement.
- D0 collector code now uses a two-stream, native-depth contract: factual `*_rgb/*_depth_m/*_seg` are same-scene render products; exact pi0.5 observation images are retained separately as `*_policy_rgb`; their differences are recorded and both streams replay exactly after prefix reset. Fixed-noise π0.5 calls fail closed unless server metadata declares internal `(10,32)` noise, and metadata/noise/action hashes plus candidate-call ordinals are persisted. No new D0 bank may use the old `get_real_depth_map` path.
- `d0_native_depth_pi05_sanity_20260918T062000Z_2271000` and `d0_native_depth_pi05_sanity_20260918T062200Z_2272000`: preserved pre-collection failures, no candidate NPZ/manifest accepted. First stopped at the intentionally strict policy-observation-vs-later-render RGB exact check (86/150528 elements, max 22), leading to the two-stream correction above. The corrected retry stopped inside MuJoCo native segmentation rendering with `IndexError: index 9761936 is out of bounds for axis 0 with size 87`; no depth/seg pairing is inferred from that failure. Own fixed-noise π0.5 server PID 2260452 was stopped (exit 143); GPU2 rechecked 0 MiB/0%, no compute process. Next D0 gate is native segmentation renderer stability under the actual 224px factual-bank call order.
- Renderer-depth follow-up: native Python segmentation conversion also fails in scripted CPU reproductions, and native depth subsequently returned near-far-plane values (~529 m) while identical `mj_ray` intersections were 0.8--3 m. Treat both native renderer buffer products as invalid for D0 in this call path. No D0 bank, repair training, WAM pairing, or VLA claim may use them. The next permissible depth implementation is analytic `mj_ray` depth with a stored valid-pixel mask and per-pixel renderer-segmentation geometry-ID agreement; it must be independently reviewed and run as a new gate before data collection.
- `analytic_ray_depth_probe_20260918T064000Z_2279000`: preserved `ERROR` before environment construction because personal `libero-render` lacks `torch`; this is an environment-prefix diagnosis only, not a ray or rendering result. The corrected personal `libero-eval-py310` invocation produced `analytic_ray_depth_probe_20260918T064500Z_2280000`: `PASS_ANALYTIC_RAY_DEPTH_ONLY` on CPU OSMesa. It re-rendered RGB/segmentation and re-read K/T exactly twice at each of two ticks for both cameras, then used the locked top-down K/T convention, 3x3 stable renderer-geom boundary exclusion, frozen `mj_ray` group mask, and exact ray/seg geom-ID match. Eligible-pixel counts were 58,056/58,272 then 58,046/58,193; agreement and valid coverage were exactly 1.0 and both vertical-flip controls were rejected. It does not validate collision, penetration, contact, WAM repair, or VLA.
- Fresh-code review recorded GO for the analytic module and its D0 collector integration. The collector is now schema `wam2repair-physical-bank-v7-analytic-mj-ray-depth-per-frame-exact-id-gated`; it never instantiates `mujoco.Renderer`. Raw robosuite segmentation is flipped to top-down solely for analytic rays, then depth, validity mask, eligible mask, and ray geom IDs are flipped back for raw-stream storage. Finite depth is allowed only at exact-ID valid pixels; all other depth cells are NaN, and the mask contract is checked before NPZ write. Ray IDs remain simulator diagnostics/GT and are not model inputs.
- `d0_analytic_ray_scripted_sanity_20260918T065000Z_2281000`: `COLLECTED`, CPU OSMesa, one scripted candidate × two frames × two cameras; explicitly excluded from D0 claims, training, validation, and test. Both frames/cameras pass the exact-ID per-frame gate and vertical-flip negative control. Agentview has 116,259 valid pixels across frames, wrist 115,782; every valid depth is finite, every invalid depth is NaN, and every valid ray geom ID equals the matching raw segmentation geom ID. This is the new analytic label-path sanity, not a policy or physical-result bank. Next: after an immediate GPU eligibility check, launch/reuse a verified fixed-noise π0.5 service and collect the smallest openpi factual-bank sanity under the v7 contract.
- `d0_analytic_ray_pi05_sanity_20260918T065500Z_2282000`: `COLLECTED`, the first smallest OpenPI-conditioned v7 factual-bank sanity (one π0.5 candidate × one action × two stored frames). It has fixed internal noise shape `(10,32)`, noise SHA `341e2c…f2cd0`, action-call ordinal 0, policy action SHA `beaff2…3c52e`, paired qpos/qvel reset error 0, and candidate action SHA `dea194…6c015d`. Agentview/wrist valid pixels total 116,265/115,758; all per-frame exact-ID gates and vertical-flip controls pass, finite-valid/NaN-invalid depth holds, and valid ray IDs equal segmentation IDs exactly. The one-step policy trajectory has no gripper-contact frame and no success, so this is **not** a contact-rich D0 bank, train/val/test data, a VLA success measurement, or physical/WAM evidence. The dedicated fixed-noise π0.5 PID 2265386 was then SIGTERM'd after verifying it belonged to `__WAM2REPAIR_USER__` and matched port 10119; port stopped listening and GPU2 memory returned to 0 MiB. Its transient utilization during teardown must not be treated as an idle check.
- `d0_analytic_ray_pi05_h8_e6_20260918T070000Z_2283000`: `COLLECTED`, one π0.5 fixed-noise action chunk (8 actions) on episode 6 under the analytic v7 contract. qpos/qvel replay error is 0 and both cameras pass every exact-ID/vertical-flip/depth-mask check, but `gripper_contact_frames=0` and `success_after_chunk=false`. It is preserved as a non-contact, non-success factual trajectory; it cannot provide contact-rich physical supervision. A fresh low-cost screen is now required to find *object* contacts before running more dense-depth D0 capture. The original screen draft was independently rejected because it counted gripper–fixture/margin contacts; it was corrected to require gripper–movable-object `dist<=0` contacts and one exact 10-step fixed-noise chunk, then sent back for review.
- `pi05_object_contact_screen_20260918T071000Z_2284000`: preserved `ERROR`, no episode result accepted. The first execution exposed that LIBERO `obj_body_id` includes fixture `flat_stove_1`; resolving it via `objects_dict[name]` raised `KeyError` before the first policy action. This prevents fixture contamination rather than silently counting it as an object contact. The repair is to define target objects strictly as `objects_dict` keys (not `obj_body_id`), then re-review and rerun the same fixed 256px/fixed-noise screen.
- `pi05_object_contact_screen_20260918T071500Z_2285000`: `COMPLETED_CONTACT_COVERAGE_SCREEN`, CPU OSMesa with the independently reviewed 256px/fixed-noise `(10,32)` protocol. Across LIBERO spatial task 0 episodes 0--7, each one first 10-step π0.5 chunk had 0 observed gripper--movable-object `dist<=0` contacts, 0 gripper--any-geometry diagnostic contacts, and no success. This is **not** an episode-level negative label and cannot establish missing contact capability; it says only that none of these first fixed-noise chunks is eligible for contact-rich D0 capture. The next screen must explicitly reproduce longer policy prefixes and record their action/noise provenance before using an observed-contact state as a D0 prefix.
- `pi05_object_contact_prefix4_screen_20260918T072500Z_2286000`: `COMPLETED_CONTACT_COVERAGE_SCREEN` and preserves explicit 40-action prefix files. Episodes 2 and 4 observed movable-object contacts at global steps `[39]` and `[35,36,37,38,39]`; all are non-success chunks. However, `d0_analytic_ray_pi05_contact_e4_20260918T073500Z_2287000` was correctly rejected before collection: the screen had constructed `OffScreenRenderEnv(camera_depths=False)` whereas D0 uses `camera_depths=True`, causing a compiled-model XML hash mismatch. No candidate NPZ/manifest was accepted. Do not waive the hash; screen construction is now changed to `camera_depths=True`, then requires review and a fresh screen before its saved prefixes can be replayed by D0.
- `pi05_object_contact_prefix4_depthmatch_screen_20260918T074000Z_2288000`: a fresh `camera_depths=True` screen found episode 4 object contacts at `[36,37,38,39]`, but its D0 replay was again correctly rejected before collection. A two-fresh-instance diagnosis showed `OffScreenRenderEnv` samples fixture placement during construction: unseeded same-config XML SHA differs (first diff is `wooden_cabinet_1_main` pose), whereas `np.random.seed(7)` *before construction* gives the exact same SHA twice and seed 8 changes it. Both screen and D0 are now changed to seed NumPy before environment construction; no old unseeded screen provenance may be replayed. This is an environment reproducibility correction, not a relaxation of provenance.
- `pi05_object_contact_prefix4_seeded_screen_20260918T075500Z_2290000`: `COMPLETED_CONTACT_COVERAGE_SCREEN` under construction-time seed and `camera_depths=True`. Episode 4 again has movable-object contacts at global prefix steps `[36,37,38,39]`; episode 2 has none. The screen’s saved 40-action prefix passed all explicit action-file/result/SHA/episode/suite/task/seed/256px/BDDL/compiled-model bindings.
- `d0_analytic_ray_pi05_contact_seeded_e4_20260918T080000Z_2291000`: `COLLECTED`, first contact-rich OpenPI factual v7 record: screened explicit 40-action prefix (replayed, never re-inferred), then one fresh fixed-noise π0.5 8-action candidate. Prefix replay has qpos/qvel reset error 0 and its manifest explicitly limits reproduction to the prefix state, not screen continuation noise. Nine factual frames have contact indicators `[1,1,0,0,0,0,1,1,1]` (5 contact frames); no task success. Both cameras pass all 9 exact-ID and vertical-flip gates, valid coverage=1.0 per frame, finite-valid/NaN-invalid depth, and exact valid ray-ID/seg-ID equality. This opens only the factual contact-data part of D0; sample count remains one candidate/one episode and it is not a D0-complete bank, physical-repair result, WAM pairing, or VLA result. Next gate: genuine WAM action-conditioned future pairing against this factual sequence, with WAM input preprocessing/timing verified separately.
- `d0_wam_pair_contact_e4_20260918T081500Z_2292000`: `PACKED_INPUT_TARGET_SEPARATED` and independently integrity-checked. The only WAM-readable input is `wam_input/window_0000_input.npz` with `{current_rgb:(224,448,3), proprio:(8), actions:(8,7)}`. The factual target is physically separate at `privileged_factual_target/window_0000_target.npz`, holding 5 frames/times/contact/pose/success at fixed offsets `[0,2,4,6,8]`; both file SHA256 values and D0 candidate/action provenance are bound in the manifest. Input/target directory paths and input allowlist are explicit, so a subsequent generator must use that exact input rather than globbing. This is a one-pair packing gate, not generated WAM output, repair training, physical accuracy, or VLA evidence.
- `wam_allowlisted_contact_e4_20260918T083000Z_2294000`: preserved pre-generation environment failure. The newly reviewed allowlisted generator was started only after an idle GPU check, but FastWAM/model resolution attempted a ModelScope VAE download under `/home/__WAM2REPAIR_USER__/checkpoints`, which violates the no-network/no-new-server-install rule. Its own PID 2277797 was immediately SIGTERM'd after owner/command verification; GPU2 returned to 0 MiB/no compute and no completed downloaded file was found. No WAM video/result was accepted. This is a local VAE-path/configuration blocker, not a WAM failure; do not retry this configuration until it is pinned to an existing personal VAE asset with network path disabled.

## 2026-09-17 21:51 对照与近期顺序修订

## 2026-09-18 π0.5 downstream probe 与 B0 重放诊断

- `pi05_imagined_probe_20260918T052001Z_2251648` 已完成：factual/raw/repair 三条件真实成功，prefix 后步数为 60/67/65。固定内部 π0.5 `(10,32)` diffusion noise、exact qpos/qvel/time/RGB replay、repair-only artifact 与全链路哈希均保存。该单个已查看 context 只证明显式 imagined-frame substitution 可运行及下游敏感性；result-to-claim=`no`，不报告 VLA 增益。详见 `PI05_IMAGINED_FRAME_PROBE_RESULTS_20260918.md`。
- 该 probe 前保留两次失败：逻辑 `/home` 与物理 `/mnt` 路径别名断言失败；以及错误向 π0.5 传入外部 `(10,7)` 而非内部 `(10,32)` noise。二者已定位、修正、复核后重跑；专属服务已关闭并确认 GPU2 释放。
- `geometry_ray_replay_20260918T052838Z_2253833` 为 CPU 诊断 `ERROR`：在原生 robosuite/OffScreenRenderEnv 中调用的是与该 binding 不兼容的 `mujoco.mj_ray`，抛出 binding TypeError。此前跨进程 agentview RGB 已知不 exact（822/196608，max 121），不能当 B0 pass。本错误不等同 ray/depth 物理失败；下一步需改用同一 robosuite binding 的 ray API，并重新运行诊断，仍不采 D0/不训练。

采用 `EXPERIMENT_PLAN_20260917_215105.md` 顶部修订。新增的是计划，不是已运行结果。

| Run | 内容 | 状态 |
|---|---|---|
| R4D-01a | 原子快照重复性 | DONE（仅窄范围；审计WARN） |
| R4D-01b | 射线/深度与解析距离unit检查 | CODE_WRITTEN_NOT_REVIEWED_OR_RUN |
| R4D-01b-run | 射线/深度与解析距离unit检查 | FAIL_RAY_RASTER_GATE：object agentview p95 7.443mm>5mm，9/12通过；保留失败，先诊断 |
| R4D-01c | 实际mesh/投影/接触标签标定 | TODO |
| R4D-01d | D0 8完整开发轨迹 | TODO，0/8 |
| R4D-02a | 真WAM配对、错误覆盖、独立解析校准 | TODO |
| R4D-03a | 统一4D/RGB/no-WAM小规模能力 | TODO |
| R4D-FT | 直接WAM轻量微调接口/profile/强监督公平对照 | PLANNED，尚未实现 |

π0.5旧服务已按用户要求关闭。无新GPU训练。正式D1/B2等待前序，不把单位几何测试写成WAM结果。

## 2026-09-17 21:05 LIBERO-only B0 前置执行

用户确认先做 LIBERO。`atomic_snapshot_20260917T130232Z_2218503` CPU 已完成：两场景各3快照，同状态重复渲染/几何读取 exact，四任务索引0/7/6/5、各50初始状态确认。详见 `ATOMIC_SNAPSHOT_RESULTS_20260917_210500.md`。
R4D-00 任务盘点推进；R4D-01 状态 PARTIAL，仅原子快照重复性通过，投影/collision/完整 D0 未完成。完整v2轨迹0/8，无新训练。旧下表TODO由本条细化，不表示全部B0已过。独立结果审计进行中。

## 2026-09-17 用户确认4D主线：数据v2与网络设计（最高优先级）

用户要求深度服务于4D/接触/穿透，先设置数据集与后续网络。采用新版 `EXPERIMENT_PLAN_20260917_201637.md`；不再把辅助depth小批调参当完整主线。

| Run | 内容 | 状态 | 证据/下一动作 |
|---|---|---|---|
| R4D-00 | v2数据契约、任务/资产初查、网络方案 | 设计已落盘；资产部分盘点 | 本地BDDL候选存在，models目录仅fastwam/openpi；未验证depth estimator其他缓存/权重可用性 |
| R4D-01 | 同步快照/几何投影/collision单元测试及D0 8轨迹 | TODO | 先解决geometry与cached obs同刻问题，不复用旧数据作4D认证 |
| R4D-02 | 真实WAM配对与非特权解析器校准 | TODO | D0通过后；未来GT不作为输入 |
| R4D-03 | 统一4D几何→RGB最小训练 | TODO | 数据/解析/输出耦合gate通过后 |
| R4D-04 | D1 96轨迹目标与sealed test | PLANNED | 尚未采集；先固定task/init-state与去重split |

新增 `implementation/dataset_v2_contract.json` 状态DESIGN_ONLY_NOT_COLLECTED、实际v2轨迹=0。旧结果保留为legacy exploratory；旧test不用于新确认。

## 2026-09-17 区域深度监督（最新追加）

- CPU首次post-step RGB检查FAIL保留；官方同周期element sensor重试 `region_targets_20260917T115643Z` PASS（13时刻/26RGB exact）。
- 三配置train-only各300步 `depth_sanity_20260917T115743Z` 已完成。RGB-only/global-depth/ROI-depth区域RGB MAE分别0.11034364/0.11181087/0.11681442，区域depth log MAE分别1.33159/1.14672/0.48663。
- 深度目标下降没有转化为RGB优于对照；不可声称物理修复。GPU3已释放，未改GPU2旧服务。
- 详见 `../REGION_DEPTH_RESULTS_20260917_115743.md`。完整性审计待返回；下一步共享梯度诊断和几何输出约束。
- 审计最终已返回WARN，见 `../REGION_DEPTH_AUDIT_20260917.md/.json`；无RGB/物理/控制增益claim，不能把COMPLETED/learnability当作胜过基线。

## 2026-09-17 深度辅助训练诊断（最新追加）

- `depth_sanity_20260917T100228Z`：两组各300步已完成，GPU3释放；3 train窗口、seed17、同结构初始化，224×448。
- RGB-only/RGB+depth末步RGB MAE=0.06257361/0.06803094；raw=0.07630026，copy=0.05939943。没有深度带来RGB提升的证据。
- 带深度组depth log MAE 0.90256941→0.71716857，梯度连通验证通过。`learnability_check=true`仅优化检查，不是泛化/物理/控制PASS。
- 位置/旋转/接触/SDF尚未加入；完整性审计另行记录。见 `../DEPTH_SANITY_RESULTS_20260917_100228.md`。

## 2026-09-17 深度几何监督数据 gate（追加最新）

- `geometry_train_gate_20260917T094500Z`：`PASS_DATA_ALIGNMENT_ONLY`，8 train 轨迹/77 窗口，CPU 实际运行退出码 0。
- 已验证 hashes、RGB/动作/时间/位姿/接触逐项对齐、正有限深度、SO(3)、相对位姿和相机矩阵基本有效性；独立 target 导出，不读 val/test 数据数组。
- 部署前独立代码审查的完整性阻塞已修复并复核通过。未训练新模型，未证明穿透/接触修复或 VLA 收益。
- 详情 `../GEOMETRY_GATE_RESULTS_20260917_094500.md`；下一步投影约定见证与 RGB+几何监督最小训练，SDF 穿透及输出 RGB 独立物理认证仍待实现。

## 2026-09-17 统一视频修复留出pilot（最新）

| Gate | 状态 | 实际证据 |
|---|---|---|
| 官方π0.5/LIBERO | 已执行 | 官方checkpoint16文件hash通过，16真实轨迹；8policy成功、8hold-open失败。不是benchmark成功率 |
| factual/WAM配对 | 已执行 | 151窗口，16动作→5帧，train/val/test=77/38/36，4/2/2组；split与hash独立检查无重复/交叉 |
| 统一视频单批学习 | 已执行 | 旧3个真实仿真诊断片段的单批学习通过，仅学习能力，不是方法优势 |
| 统一视频留出训练 | 已执行，主基线结果负面 | video_holdout_20260917T050816Z；repair/no-WAM各1000steps，validation选模；test MAE raw0.0515954、repair0.0358492、copy0.0334607、no-WAM0.0334493 |
| 正确未来压力测试 | 已执行，未证明保留 | oracle-clean identity MAE0.0336072；低像素误差raw仅4窗，不等同物理正确 |
| 失败未来保留 | 部分检查完成 | 26失败episode窗像素误差及修改幅度已统计；36窗AI盲审进行中，人工盲化标注未做 |
| 审计/结论门禁 | 进行中 | fresh-agent experiment-audit和result-to-claim；不得自代审查结论 |
| 物理错误修复、动作排序、闭环收益 | 未验证 | 不从pixel MAE或旧synthetic gate推出 |

主报告 `../VIDEO_PILOT_RESULTS_20260917.md`；数值源 `../artifacts/gpu_gate_20260917/holdout_result.json` 与 `holdout_statistics.json`，只有2 test episode和1训练seed，CI仅探索性。repair/no-WAM checkpoint各181KiB已确认存在；所有主结果仍限simulation_only。恢复入口 `../UNIFIED_VIDEO_REPAIR_GOAL_20260917.md`。

## 2026-09-17 第一轮真实视频 repair 单批学习

- 已按16控制步/5帧（0,4,8,12,16）对齐3个真实仿真片段，来源为同一π0.5 base失败episode；input/GT分别存储，WAM生成不读取target。
- `video_overfit_20260917T040644Z`，统一动作条件3D卷积视频修复：300步、112x224，单批loss 0.41498→0.28983，raw MAE 0.08697→repair 0.07427。学习sanity通过。
- 简单基线仍更好：copy current MAE=0.05816，anchored=0.06386。该实验不是泛化或物理/VLA收益证据；所有样本同episode、无夹爪接触，不能充当留出集。
- 官方π0.5 LIBERO权重已在本地完整下载并校验，正在离线传入个人服务器。详情与运行handle见 `../UNIFIED_VIDEO_REPAIR_GOAL_20260917.md`。

## 2026-09-17 GPU goal：修正 WAM 加载与 π0.5 基线

- Goal active，详见 `../UNIFIED_VIDEO_REPAIR_GOAL_20260917.md`。
- VAE 实际图像重建：37.1353 dB，权重匹配；首次旧 API 调用失败已保留。
- 找到旧推理配置覆盖 `skip_dit_load_from_pretrain=true`，诊断 provenance 确认视频/动作 backbone 为 SKIPPED_PRETRAIN，只有四组接口参数得到恢复。旧下游数字不得解释成有效预训练 WAM 的物理幻觉证据。
- 显式加载基础权重后，`video_diagnostic_20260917T035357Z` 已生成可辨认的 5 帧视频，视觉生成 sanity 通过；动作敏感性、时间对应及真实动态准确性仍待验证。
- base π0.5 最小 rollout：spatial/task0/episode0/seed7，220 步失败，0 gripper-contact frames。未建立 baseline 能力；正在本地下载官方 pi05_libero 权重，用官方对象 MD5/CRC32C 校验后离线转移。
- 未运行统一视频修复训练，未形成 WAM/VLA 改善结论。

## 2026-09-17 CPU 视频修复数据 gate

用户确认后续研究目标为 WAM 未来视频修复，VLA 切换 π0.5。已完成旧数据盘点（36 端点对 / 18 源样本 / 7 episode 分组）和一个 CPU 动作重放（两相机起末图像 MSE=0）。五个实际查看的 WAM 样本为全局彩色纹理失真，不能称已验证穿模；旧数据缺完整视频，暂不准入视频修复训练。GPU 均忙，无新 GPU 请求。详见 `../CPU_DATA_GATE_20260917.md`。后续优先验证 WAM 生成管线、动作/视频时间接口与 π0.5 baseline，早期状态向量 toy PASS 不能替代这些 gate。

## 2026-09-17 recovery: CPU factual alignment

- Personal OSMesa runtime in `.aris/compute/direct-ssh-__REMOTE_HOST__.md` was independently revalidated with the documented invocation: exit 0, RGB/depth witness matched. EGL itself remains unresolved; CPU OSMesa bypasses it without system changes.
- All four GPUs were busy at preflight (approximately 31/31/39/31 GiB, 99–100% utilization). Existing OpenPI PID 2143996, tmux `wam2repair_openpi`, port 10098 were present. No inference request or new GPU job was launched.
- `factual_cpu_20260917T022452Z`: COLLECTED, alignment validation PASS. Real LIBERO spatial task 0, initial episode 0, seed 7; four scripted candidates × eight simulator actions, nine saved frames per candidate. Identical initial qpos/qvel (error 0), finite RGB/depth/poses, SO(3), relative transforms, timestamps, paired resets, checksums and contact symmetry checked.
- Evidence: `__WAM2REPAIR_ROOT__/results/wam2repair/factual/factual_cpu_20260917T022452Z/{manifest.json,alignment_validation.json,candidate_00.npz,...}`; log `__WAM2REPAIR_ROOT__/logs/wam2repair/factual_cpu_20260917T022452Z.log`.
- This is **sanity-only factual simulator data**, excluded from train/validation/test. Candidates are scripted, not VLA-generated. All four have zero gripper-contact frames and no final success; slip labels are unknown (-1). No repair overfit, Stage-2, action ranking or closed-loop performance gate has passed from this run.
- Next: collect contact-rich factual trajectories and VLA candidates when GPU eligibility permits; define episode-grouped splits and known/unknown label masks, then factual single-batch overfit. Stage-2 still requires genuine WAM futures and a non-privileged state extractor.

## 2026-09-16 execution update

| Run | Status | Evidence |
|---|---|---|
| W2R-M0 / E0 | PASS | Single-batch synthetic repair on GPU 2. Pose RMSE 0.08210→0.03285; relative-pose RMSE 0.07118→0.03103; contact/slip accuracy 1.0/1.0. `__WAM2REPAIR_ROOT__/results/wam2repair/e0/e0_sanity.json` |
| W2R-M1a / Stage 1 | FAIL, paused | Three actual runs (including clean v2 reimplementation) did not meet validation contact/slip and loss gates. Latest: `__WAM2REPAIR_ROOT__/results/wam2repair/stage1/stage1_v2_retry2.json`; logs: `__WAM2REPAIR_ROOT__/logs/wam2repair/` |

M1 gate is not passed; Stage 2, M2, M3 and M4 remain pending. No WAM/VLA performance claim is made.

Additional M1 retries (weighted classification and reduced corruption sanity) also failed the predeclared validation gate; latest evidence is `__WAM2REPAIR_ROOT__/results/wam2repair/stage1/stage1_v2_retry4_easy.json` with log `__WAM2REPAIR_ROOT__/logs/wam2repair/stage1_gpu3_v2_easy.log`.

### M1a repair retry

Stage-1 v3 passed the synthetic gate after correcting the raw baseline, using a residual repair model, separate contact/slip heads, and selecting the best validation checkpoint. Evidence: `__WAM2REPAIR_ROOT__/results/wam2repair/stage1/stage1_v3_retry.json`; log: `__WAM2REPAIR_ROOT__/logs/wam2repair/stage1_gpu3_v3.log`. This PASS is limited to synthetic Stage-1 corruption and does not establish WAM/VLA gains.

详见 [EXPERIMENT_TRACKER_20260915_000000.md](EXPERIMENT_TRACKER_20260915_000000.md)。
## 2026-09-18 raw WAM-to-factual contact-window evaluation (no copy baseline)

### Additional episode-separated raw pair: e25

- The first two e25 launcher attempts are retained as failed recoveries: multi-word language was split by remote-shell quoting. A subsequent attempt correctly preserved the language but pre-created the generator output directory, which the generator rejects by design. Neither attempt loaded a model or generated an output.
- Recovery `pilot_v2_wam_e25_20260918T122000Z_2303107` used a SHA-bound input-only pair and pre-existing personal offline FastWAM assets. It generated a five-frame video with result status `GENERATED_INPUT_ONLY_REQUIRES_FACTUAL_EVAL`; its GPU process subsequently exited and released GPU 2.
- Strict post-generation evaluation `raw_wam_factual_eval_contact_e25_20260918T123000Z_2303201` verified pair input, privileged target, generated video SHA, shape, offsets `[0,2,4,6,8]`, times, and metadata before reporting raw RGB MAE `15.9083853` (0--255). It is explicitly `EVALUATED_RAW_WAM_VS_FACTUAL_NOT_PHYSICAL`: this number does not establish contact, penetration, pose, repair, action-ranking, or VLA performance.
- A second independent pair completed with the corrected launcher: `pilot_v2_wam_e27_20260918T124000Z_2303108` and `raw_wam_factual_eval_contact_e27_20260918T125000Z_2303202`. The same fail-closed provenance/shape/timing checks passed and raw RGB MAE was `12.9107418` (0--255). It has the same non-physical, non-repair, non-VLA interpretation.
- The same completed sequence for e28/e29 is `pilot_v2_wam_e28_20260918T126000Z_2303109` / `raw_wam_factual_eval_contact_e28_20260918T127000Z_2303203` (raw RGB MAE `17.4731369`) and `pilot_v2_wam_e29_20260918T128000Z_2303110` / `raw_wam_factual_eval_contact_e29_20260918T129000Z_2303204` (raw RGB MAE `17.5739212`). Both are input-only generations followed by strict factual comparisons, with no physical or VLA interpretation.
- Target-only analytic-depth geometry bridges were built for e17/e25/e27/e28/e29 with status `BUILT_CONSERVATIVE_TARGET_ONLY_GEOMETRY_BRIDGE`. Coverage is about 0.902--0.911 over the five frames. They are supervision artifacts only, not repair outputs or geometry certification; a fresh, predeclared heldout episode is still required before any expanded heldout training claim.

## 2026-09-18 predeclared train/validation/heldout geometry pilot

- Predeclaration is in `../EXPANDED_GEOMETRY_SPLIT_20260918.md`: e34/e36 train, e37 validation, e38 heldout, e39 reserve. It was written before D0/WAM/factual target construction for episodes 34--38. No repeated-current-frame baseline is included.
- e34/e36/e37 were collected with explicit audited 40-step fixed-noise pi0.5 prefix replay. Their candidate contact-frame counts were 8/5/6 and reset qpos/qvel errors were 0. Input/privileged-target-separated pairs, input-only offline FastWAM futures, raw factual diagnostics, and target-only analytic-depth geometry bridges all completed. Raw WAM RGB MAE values were e34 `11.3197088`, e36 `17.9394798`, e37 `16.0503902` (0--255); these are explicitly non-physical raw diagnostics.
- New selection artifact `geometry_selection_train34_36_val37_20260918T144000Z_2306400` accepts exactly two train and one validation episode and has no heldout argument. It selected step 400 with status `SELECTED_CHECKPOINT_WITHOUT_HELDOUT_ACCESS`; its frozen checkpoint SHA is bound in `result.json`.
- Only after that selection did e38 D0 collection, input-only WAM generation, and target-only geometry bridge occur. `frozen_geometry_eval_train34_36_val37_test38_20260918T149000Z_2306500` verifies the frozen selection/checkpoint SHA and evaluates e38 once: future RGB MAE raw `0.05425926` -> repair `0.04898077`; temporal MAE raw `0.03424188` -> repair `0.03309106`; predicted-depth log MAE `0.77580923`, AbsRel `3.34396911`. e38 had one factual contact frame in the sampled candidate.
- This is a **one-test-episode exploratory RGB/temporal + target-only-depth pilot**. It does not certify repair of contact, penetration, object 6D pose, rotation, or physical consistency; it does not show WAM improvement, action ranking, pi0.5 closed-loop success, or VLA benefit. Raw WAM has no depth output, so depth error has no raw-WAM-depth comparison.

## 2026-09-18 unified 4D-head exploratory heldout gate

- `../UNIFIED_4D_HEAD_GATE_20260918.md` predeclared a subsequent, separate gate before e39 D0/WAM/geometry construction: e34/e36 train, e37 validation, e39 heldout. The unified model inputs remain raw WAM RGB, current RGB, actions and proprio only. Its five heads are repaired RGB, depth, contact matrix, task-0 object position, and task-0 rotation-6D.
- `unified4d_selection_train34_36_val37_20260918T150000Z_2306600` selected step 400 with status `SELECTED_UNIFIED4D_CHECKPOINT_WITHOUT_HELDOUT_ACCESS`. The selection program has no heldout-path argument; all fixed loss weights are recorded in the predeclaration. e39 was collected/generated only afterward.
- e39 factual candidate had **zero** contact frames and no success; it was retained rather than replaced. `unified4d_eval_train34_36_val37_test39_20260918T155000Z_2306700` binds the frozen checkpoint and e39 pair/WAM/geometry SHA. Metrics: RGB raw `0.06175469` -> repair `0.05574813`; temporal raw `0.03824612` -> repair `0.03657455`; depth log-MAE `0.60654163`; contact BCE `0.08101590`, accuracy `0.96875`; position MAE `0.02577355`; rotation-6D MAE `0.03634409`.
- This is an exploratory per-task supervised multi-head prediction check only. In particular, e39's all-negative contact target makes accuracy class-imbalanced and **not evidence of contact recovery**. Neither these state-head errors nor RGB metrics certify penetration, physical consistency, or 6D image repair, and no action-ranking/closed-loop pi0.5/VLA conclusion is supported.

## 2026-09-18 offline action-ranking collection incident

- The predeclared e40 four-candidate collection `d0_action_rank_e40_k4_20260918T157000Z_2307000` stopped during candidate 02. Candidates 00 and 01 were retained as partial artifacts (each had qpos/qvel reset error 0), but the run is invalid for the K=4 ranking gate and will not be scored.
- Cause: the frozen analytic depth contract requires exact geometry-ID agreement for every 3x3-stable eligible pixel. One candidate-02 frame had 56,901/56,902 agreement (one boundary pixel), so `require_exact_alignment` failed closed. No tolerance was relaxed, no partial-candidate ranking will be computed, and this is not interpreted as a policy, WAM, repair, or physical result.
- A prewritten recovery amendment fixes exactly one independent retry to episode 42, the next numerically scheduled contact-positive screened episode. It retains e40 as failure and preserves K=4/all-candidates/no-retuning requirements; see `../OFFLINE_ACTION_RANKING_GATE_20260918.md`.

## 2026-09-18 e42 frozen offline action-ranking diagnostic

- The predeclared recovery collection `d0_action_rank_e42_k4_20260918T160000Z_2307001` completed all four candidates under the unchanged exact analytic ray/raster contract. All candidates have eight actions, zero qpos/qvel reset error, nine sampled gripper-contact frames, and no chunk success. Four SHA-bound input/target-separated pairs, four input-only FastWAM futures, and four target-only geometry bridges were then built; no e42 item entered checkpoint selection.
- The frozen `unified4d_selection_train34_36_val37_20260918T150000Z_2306600` checkpoint was applied in `offline_rank_unified4d_e42_k4_corrected_20260918T164500Z_2307401`. Its prediction ledger is saved and SHA-bound before any factual geometry target was opened. Predicted contact scores for candidates 0--3 were 0.1809283, 0.1809966, 0.1811879, 0.1806861.
- All four factual future gripper--movable contact-edge counts were exactly 4. Consequently the factual ranking is tied and non-discriminative. The corrected artifact reports `EVALUATED_FROZEN_OFFLINE_CONTACT_RANKING_UNINFORMATIVE_TIED_FACTUAL`, with top-1 match and Spearman explicitly null. The earlier same-data artifact, which ordinally broke the factual tie by candidate index, is retained but superseded and must not be interpreted.
- This is not an action-ranking success/failure, physical-contact-repair result, pi0.5 closed-loop result, or VLA benefit. A future ranking gate needs a fixed candidate set with factually varying contact or task-outcome utility, while retaining all candidates and the same prediction-before-factual accounting.

## 2026-09-18 e44 pre-contact candidate-utility data gate

- The first e44 pre-contact collection attempt was rejected before environment construction because the v7 collector correctly requires an explicit prefix file to be SHA-bound by a completed contact-coverage screen. No D0 candidate or manifest was created. Rather than weaken this guard, `screen_pi05_contacts.py` was extended with an exact `--prefix-steps` mode: it stores only actually consumed fixed-noise actions and records a truncated final chunk. The independently completed 32-step e44 screen has zero prefix contacts and its action file is accepted by the unchanged collector.
- `d0_action_rank_e44_precontact_k4_retry_20260918T168000Z_2307601` then completed K=4 under the same exact analytic ray/raster gate. Every candidate has eight actions, qpos/qvel reset error 0, no chunk success, and six sampled gripper-contact frames. All four were retained.
- Before any WAM generation, the predeclared factual utility feasibility check found the five-frame future gripper--movable contact-edge count equal to 3 for every candidate, with identical per-frame counts `[0,0,1,1,1]`. This makes the factual ranking non-discriminative. Per the gate, no WAM video, repair output, or ranking metric is generated/reported for e44; this avoids spending GPU compute on a label that cannot test action selection.
- Together with e42, this establishes a data-design limitation of the current fixed-noise π0.5 candidate stream at these states: it can produce distinct action arrays but not distinct short-horizon contact-edge utility. It is not evidence for or against repair, physics, ranking, or VLA benefit. A next ranking design must predefine a richer factual utility (e.g., pose progress / task predicate) or a candidate-generation protocol that yields outcome variation before WAM evaluation.

- `wam_allowlisted_contact_e4_nodownload_20260918T084000Z_2295000` generated one genuine action-conditioned FastWAM video from the SHA-bound input-only pair. The run used only pre-existing personal FastWAM assets with offline/no-download settings. Its `raw_wam_video.npz` and input SHA were verified before factual evaluation.
- `raw_wam_factual_eval_contact_e4_20260918T090500Z_2298000` independently bound the generation result, paired input, privileged target, video SHA, five-frame shape, exact offsets `[0,2,4,6,8]`, increasing simulator times, and all factual metadata dimensions before calculating metrics. Status: `EVALUATED_RAW_WAM_VS_FACTUAL_NOT_PHYSICAL`.
- Raw WAM-vs-factual RGB MAE (0--255) was 18.1222 overall, with per-frame MAE `[1.9558, 12.4081, 21.5606, 26.0948, 28.5917]`; temporal-delta MAE was `[12.2986, 14.9106, 12.5979, 11.9915]`. This one contact-rich window establishes neither penetration/contact correctness nor a repair/VLA improvement. A visual audit sheet is retained in that result directory.
- No copy-current-frame / repeated-frame baseline was run or reported. The next evidence requirement is multiple episode-separated raw-WAM/factual pairs with geometry labels, then a repair-only training/held-out gate.
## 2026-09-18 real raw-WAM repair tiny pilot (no repeated-frame baseline)

- Three strict input/target-separated v2 pairs were rebuilt with exact source episode identity: LIBERO-spatial task 0, episodes 4 and 14 for training and episode 15 held out. Pair construction rejects incomplete suite/task/episode identity; the pilot rejects duplicate train source episodes and any heldout/train source episode or D0-manifest overlap.
- Each FastWAM prediction was generated input-only with local personal assets and offline/no-download settings. Before factual supervision, the pilot verifies input/target/video SHA, five-frame shape, registered offsets `[0,2,4,6,8]`, increasing times, and all factual metadata dimensions. Target/contact never enter the repair-model input.
- Reproducible result: `real_wam_repair_pilot_v2a_episodes4_14_to15_20260918T085000Z_2287400`, status `EVALUATED_EPISODE_ISOLATED_RGB_PILOT`. Heldout episode 15 has simulator contact metadata on all five sampled frames. Future RGB MAE raw `0.04250625` -> repair `0.03528053` (absolute `-0.00722572`, about 17.0% relative); temporal-delta MAE `0.02510968` -> `0.02354598` (about 6.2% relative). No copy-current/repeated-frame baseline was run.
- The artifact records SHA256 for both heldout prediction NPZ and repair checkpoint. Independent audit: WARN, not FAIL. This is only two train episodes plus one heldout RGB/temporal pilot; it does not demonstrate physical contact/penetration/6D correction, generalization, WAM improvement, pi0.5 action-ranking, or closed-loop VLA gain.
## 2026-09-18 contact-future data expansion after tiny pilot

- Contact coverage screen `pi05_object_contact_prefix4_seeded_screen_ep16_23_20260918T075000Z_2288000` found target-object contact at episode 17 step 39 and episode 23 steps 37--39; zero-contact episodes remain coverage evidence only.
- Episode 23 D0 v7 analytic-ray collection completed with all 9 candidate factual frames carrying gripper contact. It was packed with exact source identity `{libero_spatial, task 0, episode 23}`, generated by input-only offline FastWAM, and independently compared to factual future in `raw_wam_factual_eval_contact_e23_20260918T083000Z_2288400` (raw RGB MAE 15.8871/255). This is a new contact-future data point, not a repair or physical-correction result.
- The previously inspected episode 15 heldout result is not reused for tuning after adding episode 23. Next valid stage is a newly predeclared, episode-isolated expanded split with fresh heldout episodes and geometry/contact-specific metrics; no repeated-frame baseline will be run.
## 2026-09-18 second contact-future expansion sample

- Contact coverage screen episodes 24--31 found continuous prefix contact in episode 24 (steps 33--39), episode 28 (36--39), and episode 29 (35--38). These are screening observations only.
- Episode 24 v7 D0 candidate collection completed with contact on all 9 factual frames. `pilot_v2_pair_e24_20260918T082000Z_2292000` propagated exact episode identity; input-only offline FastWAM generation and independent factual evaluation completed at `raw_wam_factual_eval_contact_e24_20260918T083000Z_2292200` (raw RGB MAE 19.2280/255). No repair was trained/tuned on this sample.
- Episode 24 is reserved for a newly declared expanded split. Do not combine it with earlier episode 15 inspection as a fresh confirmatory test. No repeated-frame baseline was run.
## 2026-09-18 validation-selected four-episode exploratory repair pilot (no repeated-frame baseline)

- `real_wam_repair_val_holdout_episodes4_14_val23_test24_20260918T084000Z_2292400` completed with strict source identity: train `{4,14}`, validation `{23}`, heldout `{24}` within LIBERO-spatial task 0. The heldout factual target was opened only after the validation-selected checkpoint (best step 400) was saved. Pair/WAM input allowlists, SHA, shape, offsets and time contracts fail closed. Checkpoint and prediction artifact SHA are stored in result.
- Heldout episode 24 (simulator contact metadata on all five sampled frames): raw future RGB MAE `0.09002755` -> repair `0.04050286` (absolute `-0.04952469`, about 55.0%); temporal MAE `0.04420660` -> `0.02844320` (about 35.7%). No copy-current/repeated-frame baseline was run.
- Independent audit: WARN. Execution/provenance/artifacts pass, but this is only 2 train + 1 validation + 1 heldout episode and the raw heldout trajectory had been inspected for a data gate. It is an exploratory RGB/temporal result only, not evidence of generalization, contact/penetration/6D correction, WAM improvement, action ranking, pi0.5, or closed-loop VLA benefit.

## 2026-09-18 controlled counterfactual e44 ranking diagnostic (negative)

- A screened 32-action, zero-contact e44 prefix was bound to four predeclared counterfactual 8-action candidates: policy, attenuated, hold, and reverse. The candidate manifest and replayed D0 manifest have matching prefix-action SHA `1bcf654e...`; the collector was subsequently tightened to require this cross-manifest prefix binding on all future counterfactual collections.
- All four D0 candidates completed with reset qpos/qvel error 0. Their factual utility differed: policy `(contact_edges=3, -bowl_plate_distance=-0.11575835)`, attenuated `(2, -0.11570590)`, hold `(0, -0.11590894)`, reverse `(0, -0.11590894)`; none succeeded. Four SHA-bound pairs, input-only FastWAM futures, and target-only geometry bridges were generated before scoring.
- The frozen contact-head diagnostic `offline_contact_rank_counterfactual_e44_20260918T176000Z_2308400` saved prediction ledger SHA `863ba8...` before factual access. It ordered candidates `[3,2,1,0]` while factual contact ordered `[0,1,2,3]`: top-1 mismatch and ordinal Spearman `-1.0`.
- The separate predeclared combined contact-plus-relative-pose proxy `offline_combined_rank_counterfactual_e44_20260918T177000Z_2308500` likewise saved its ledger SHA `091c0b...` before factual access and produced `[3,2,1,0]` versus factual combined `[0,1,2,3]`, top-1 mismatch and Spearman `-1.0`. This is a negative one-state offline ranking result, not a physical-repair, task-success, pi0.5 closed-loop, or VLA-gain conclusion. The frozen tiny-data repair model must not be used for action selection; expand episode-isolated factual training data first.

## 2026-09-18 expanded unified-4D fresh-heldout gate

- The predeclaration `../EXPANDED_UNIFIED4D_FRESH_HOLDOUT_GATE_20260918.md` locked train `{34,45}`, validation `{48}`, and fresh heldout `{46}` before e46 D0 collection. Episodes `{36,37,39,49}` are excluded; in particular e49 had already been factually inspected and is not called a test episode.
- e45/e49 D0 candidates retained 9 sampled contact frames each; e48 retained zero future contact frames; all have zero qpos/qvel reset error and no chunk success. Their input-only FastWAM futures and target-only geometry bridges were generated with raw WAM factual RGB diagnostics of 18.5100/11.4206/18.0149 (0--255) for e45/e48/e49. Those RGB numbers are explicitly non-physical diagnostics.
- Selection `unified4d_selection_train34_45_val48_20260918T181600Z_2309300` accepts exactly two train items and one validation item, with no heldout argument. It selected step 400; its validation fixed aggregate decreased from 0.203118 at step 50 to 0.117180 at step 400.
- Only then were e46 D0, pair, input-only WAM future, and target-only geometry bridge built. Frozen evaluation `unified4d_eval_train34_45_val48_test46_20260918T182100Z_2309700` reports raw->repair RGB MAE `0.0716651 -> 0.0396035` and temporal MAE `0.0400285 -> 0.0322461`; depth log-MAE `0.758528`, contact BCE/accuracy `0.0509794/0.99375`, position MAE `0.0190020`, and rotation-6D MAE `0.0229524`.
- This remains one fresh heldout episode and a multi-head supervised prediction diagnostic only. Its contact accuracy may be imbalanced; none of these metrics certifies contact restoration, penetration correction, geometric validity in rendered images, action ranking, task success, pi0.5 closed-loop gain, or VLA benefit.
- Frozen contact-classification re-evaluation `unified4d_eval_train34_45_val48_test46_contactprf_20260918T182200Z_2309701` adds class-aware metrics without retraining or checkpoint selection: 8/320 factual contact-matrix entries are positive, precision `0.80`, recall `1.00`, F1 `0.888889`. Accuracy `0.99375` is therefore not used alone. This remains prediction against simulator contact labels, **not** certification that repaired RGB depicts physically valid contact or non-penetration.

## 2026-09-18 three-train contact-transition fresh-heldout gate

- The gate `../EXPANDED_UNIFIED4D_TRANSITION_GATE_20260918.md` locked train `{30,34,45}`, validation `{48}`, heldout `{31}` before e31 D0. e30 came from a zero-contact prefix screen but its factual 8-action future has five contact frames; it is training-only. e31 only had a prefix screen before selection and no D0/pair/WAM/geometry target.
- First launch `unified4d_selection_train30_34_45_val48_20260918T183000Z_2310400` failed before training/checkpoint creation because the generalization change left an exact-three episode uniqueness check. The failure is retained. The recovery corrected only that check to `len(train)+1`, compiled, repeated the same fixed inputs/hyperparameters with a new output, and selected step 400 at `unified4d_selection_train30_34_45_val48_recovery_20260918T183100Z_2310401`; validation aggregate decreased `0.210432 -> 0.124067`.
- Only after selection did e31 D0/pair/input-only WAM/target-only geometry run. `raw_wam_factual_e31_freshheldout_20260918T183800Z_2311000` reports raw WAM RGB MAE `20.1147/255`, explicitly a non-physical diagnostic. Frozen heldout `unified4d_eval_train30_34_45_val48_test31_20260918T183700Z_2310900`: raw->repair RGB `0.0945356 -> 0.0617439`, temporal `0.0485688 -> 0.0407800`, depth log-MAE `0.611319`, position `0.009906`, rotation-6D `0.009881`.
- e31 has only 4/320 factual contact entries positive. Although recall is `1.0`, precision is `0.4` and F1 `0.571429`; accuracy `0.98125` is not interpreted alone. This is evidence of false positives in rare-contact prediction and is not a contact-restoration, penetration-correction, action-ranking, task-success, pi0.5 closed-loop, or VLA-benefit result.

## 2026-09-18 task-relevant contact and repeated e44 ranking diagnostics

- Frozen re-evaluation now reports future gripper--movable-object edges separately from the imbalanced full 8x8 matrix. e46 has `4/20` positives and F1 `1.0`; e31 has `2/20`, precision `0.5`, recall `1.0`, F1 `0.666667`. These are tiny per-episode simulator-label diagnostics, not visual contact certification. The D0 `contact_min_distance` field was audited: zeros represent both absent contact and initialization, so it is not used as SDF or penetration evidence.
- The independently changed three-train checkpoint was applied once to the pre-existing e44 counterfactual challenge in `offline_combined_rank_e44_new3train_exploratory_20260918T184000Z_2311200`. Prediction ledger SHA `014989...` was saved before factual geometry opened. It again ordered `[3,2,1,0]` against factual `[0,1,2,3]`, top-1 mismatch, Spearman `-1.0`. This confirms the current repair-head proxy is **not** usable for action choice. No pi0.5 action was selected or executed, and no VLA claim is made.

## 2026-09-18 weighted-contact ablation (negative)

- `../WEIGHTED_CONTACT_FRESH_HOLDOUT_GATE_20260918.md` fixed the only method change before e32 collection: BCE `pos_weight=36`, derived from the 26/960 training contact-label ratio; train `{30,34,45}`, val `{48}`, fresh heldout `{32}`. e32 D0 had reset error 0, three future contact frames and no success; its input-only WAM and target-only geometry were built only after weighted selection.
- Weighted selection `unified4d_weighted36_selection_train30_34_45_val48_20260918T184100Z_2311300` chose step 250. Frozen e32 evaluation `unified4d_weighted36_eval_train30_34_45_val48_test32_20260918T184700Z_2311800`: task-relevant gripper--movable precision/recall/F1 `0.5/1.0/0.666667`, RGB `0.0718201 -> 0.0685134`, temporal `0.0378100 -> 0.0375141`, depth log-MAE `0.959489`.
- A posthoc paired evaluation of the already-frozen unweighted checkpoint with the **identical** train/val sources and unseen e32 (`unified4d_unweighted_eval_train30_34_45_val48_test32_posthocpaired_20260918T184800Z_2311801`) has the same task-relevant F1 `0.666667`, but better RGB `0.0655703`, temporal `0.0363768`, depth log-MAE `0.675083`, position `0.007472`, rotation `0.004889`. Thus this single predeclared weighted variant did not improve rare-contact classification and degraded other heldout diagnostics; it is a negative ablation, not a method component.

## 2026-09-18 depth-supervision ablation (mixed, tiny-data only)

- `../DEPTH_SUPERVISION_ABLATION_FRESH_GATE_20260918.md` predeclared an ablation with train `{30,34,45}`, validation `{48}`, and fresh heldout `{33}`. The only training change was `depth_loss_weight=0` versus the existing `0.1` depth-supervised checkpoint; neither selection program had access to e33.
- Frozen e33 evaluation of the no-depth selection, `unified4d_nodepth_eval_train30_34_45_val48_test33_20260918T185500Z_2312400`, reports RGB `0.0764787 -> 0.0481146`, temporal `0.0396162 -> 0.0319705`, depth log-MAE `1.345976`, position `0.008640`, rotation-6D `0.005366`, and task-relevant future gripper--movable F1 `0.857143` (3/20 positive labels).
- The paired e33 evaluation of the frozen depth-supervised selection, `unified4d_depth_eval_train30_34_45_val48_test33_posthocpaired_20260918T185500Z_2312401`, reports RGB `0.0531830`, temporal `0.0333784`, depth log-MAE `0.670000`, position `0.007746`, rotation-6D `0.006610`, and the same task-relevant contact F1 `0.857143`.
- Therefore, in this one fresh episode, depth supervision improves its direct depth target and position proxy, while degrading RGB/temporal metrics modestly and not changing contact classification. It is a mixed, single-episode supervision ablation—not evidence that depth repairs 4D penetration/contact in images, nor evidence of action ranking, pi0.5 success, or VLA gain.

## 2026-09-18 factual-bank expansion capacity check

- Predeclared task-0 reservoir episodes 50--57 and heldout e58 are infeasible because `libero_spatial` task 0 has exactly 50 initial states (`0--49`). Screen launch `pi05_screen_e50_58_prefix40_20260918T120100Z_2371800` exited before any inference, D0 collection, target construction, WAM generation, or metric. The protocol and error are retained in `../EXPANDED_FACTUAL_BANK_E50_58_GATE_20260918.md` and are superseded rather than silently edited.
- The replacement task-1 protocol is predeclared in `../TASK1_FACTUAL_BANK_FRESH_GATE_20260918.md`: e0--7 reservoir, e8 validation, e9 heldout. Its completed fixed-noise 40-step prefix screen `pi05_screen_task1_e0_9_prefix40_20260918T120500Z_2373000` found zero gripper--object contact frames and no prefix success for all ten episodes. This is coverage evidence only; no item may be presented as contact-rich, a physical-repair result, a WAM result, or a VLA result.
- The temporary personally owned fixed-noise pi0.5 server on GPU2/port10126 (PID 2371748, verified command and owner before action) was terminated after screening; all four GPUs subsequently reported 0 MiB and 0% utilization.

## 2026-09-18 legacy MuJoCo contact-distance semantics audit

- Added and remotely compiled the read-only `implementation/audit_legacy_contact_distances.py`. It SHA-binds immutable v7 candidate files and reports strictly negative values separately from the legacy ambiguous zeros; it intentionally makes no WAM/RGB/physical-repair claim.
- Contact-positive D0 audits `d0_distance_audit_e30_20260918T121500Z_2375000` and `d0_distance_audit_e45_20260918T121500Z_2375001` found respectively 10 and 18 strictly-negative directed simulator contact entries, all also flagged in `contact_matrix` (`negative_without_contact_flag=0`). e30's smallest observed value was `-0.0016516 m`.
- The zero-contact control `d0_distance_audit_e48_20260918T121700Z_2375100` found zero negative and zero observed-contact entries. Across all three audits, hundreds of legacy zeros remain ambiguous (494--504 directed off-diagonal entries per candidate), because v7 initializes distances to zero. Thus negative values are a valid simulator-contact audit witness, while the legacy field is **not** a signed distance function, a mesh penetration label, or appropriate direct penetration supervision. A future collection schema must add an explicit observation mask and preserve NaN/no-pair semantics.

## 2026-09-18 v8 explicit contact-distance contract replay

- `collect_physical_bank.py` now writes a v8 schema with `contact_distance_observed` and `contact_signed_distance_m`: each MuJoCo-reported owned pair has a finite minimum distance, and every unreported pair is NaN. The legacy zero-filled `contact_min_distance` is retained only for backward readers. The collector validates the finite-inside/NaN-outside contract before saving. `build_geometry_factual_target.py` accepts v7/v8; v7 exports no distance target while v8 propagates the explicitly masked fields target-only.
- A fresh fixed-noise π0.5 server was started only after an immediate all-idle GPU check and used to replay the screen-bound e45 prefix. `d0v8_e45_replay_contract_20260918T121300Z_2376800` completed with qpos/qvel reset error `0`, one eight-action candidate and 7 gripper-contact frames. It is `sanity_only`, does not overwrite v7 data, and is excluded from train/validation/test.
- The v8 audit `d0v8_distance_audit_e45_20260918T121700Z_2377000` confirms 18 strictly-negative directed entries, 18 observed-contact entries, zero negative-without-contact entries, **zero ambiguous zeros**, and a minimum `-0.0054366 m`. These are simulator contact-constraint values, not SDF penetration certificates or WAM-image error labels. PID 2375934 (verified personal fixed-noise server on port 10127) was terminated after the audit; all four GPUs returned 0 MiB/0%.

## 2026-09-18 v8 input-only WAM / factual-pair compatibility check

- `build_d0_wam_pair.py` was extended to accept v7 or v8 analytic D0 without changing its input/privileged-target separation. The v8 e45 replay was packed in `pair_d0v8_e45_contract_20260918T122100Z_2378000`; the pair binds its D0 candidate SHA, contains the sole generator-allowlisted input NPZ separately from the factual target, and uses offsets `[0,2,4,6,8]`.
- First FastWAM launch `wam_d0v8_e45_contract_20260918T122300Z_2379000` failed at argparse before model loading/video creation because Windows SSH stripped the multi-word language quoting. The recovery used shell-escaped identical language and a new output directory. `wam_d0v8_e45_contract_recovery_20260918T122600Z_2379200` actually ran on preflight-idle GPU3 with existing offline personal assets, then released it; its result permits only the SHA-bound input NPZ and records no target access.
- Strict post-generation factual diagnostic `raw_wam_factual_d0v8_e45_20260918T122900Z_2380000` binds the pair, WAM result/video, target, five-frame shape, offsets and increasing simulator times. Raw RGB MAE is `18.8779/255`, with per-frame MAE `[1.6949,13.7744,22.2286,27.1079,29.5839]`. This is explicitly an RGB/temporal diagnostic, not contact, penetration, repair, ranking, task success, or VLA evidence.
- `geometry_d0v8_e45_contract_20260918T122900Z_2380001` built the target-only v8 geometry bridge (about 0.906--0.908 valid depth coverage), reporting the explicit mask/NaN distance contract. No model training or heldout conclusion used this sanity-only replay.

## 2026-09-18 clean-provenance v8 fresh-heldout diagnostic

- A collector provenance defect was found: its previous manifests hard-coded `split: sanity_only`, including the earlier v8 items. `collect_physical_bank.py` now requires an explicit `--declared-split` role for train/validation/heldout (default remains excluded sanity-only), rejects non-OpenPI declared splits, and was remote-compiled before recollection. Earlier v8 results remain integrity-limited and are not reused.
- Predeclared clean gate `../V8_CLEAN_CONTACT_DISTANCE_E10_FRESH_GATE_20260918.md` used newly collected train `{30,34,36}`, validation `{11}`, and fresh heldout `{10}`. Train manifests all say `split=train`, validation says `split=validation`, and e10 says `split=heldout`; all have zero qpos/qvel reset error. Input-only FastWAM and target-only bridges completed for train/validation before selection. e10 had no D0/pair/WAM/geometry item until checkpoint selection.
- Selection `unified4d_v8distance_clean_selection_train30_34_36_val11_20260918T134500Z_2410000` selected step 400 without e10 access. Then frozen e10 evaluation `unified4d_v8distance_clean_eval_train30_34_36_val11_test10_20260918T135800Z_2415000` gives RGB raw->repair `0.0535216 -> 0.0481600` and temporal `0.0350084 -> 0.0335985`; contact precision/recall/F1 `0.20/1.0/0.3333` on 2/320 positives, task-relevant F1 `0.4` on 1/20 positive, and observed-pair masked distance MAE `0.0030836 m` on 2 entries.
- This is a clean single-episode supervised-prediction diagnostic, not physical visual repair proof. The distance is MuJoCo-reported contact constraint distance, not SDF/mesh penetration; rare positives and false positives prevent a contact-restoration claim. No action selection, task success, pi0.5 closed-loop, or VLA benefit is claimed. The personally owned port-10132 server was verified then terminated; GPUs 0--3 returned 0 MiB/0%.

## 2026-09-18 v8 contact-rich distance diagnostic (integrity-limited)

- `../V8_CONTACT_RICH_DISTANCE_FRESH_GATE_20260918.md` predeclared train `{30,45}`, validation `{46}`, and e43 as fresh heldout before e43 D0/pair/WAM/target construction. e46 had five contact frames and four observed directed distance entries in validation. Selection `unified4d_v8distance_selection_train30_45_val46_20260918T132800Z_2395001` selected step 350; its validation metrics are too small to establish a selection conclusion.
- Only after selection, e43 was collected, paired, generated by input-only FastWAM, and target-bridged: `d0v8_e43_contactrich_freshheldout_20260918T133300Z_2396000`, `pair_d0v8_e43_contactrich_freshheldout_20260918T134200Z_2397000`, `wam_d0v8_e43_contactrich_freshheldout_20260918T134400Z_2398000`, and `geometry_d0v8_e43_contactrich_freshheldout_20260918T134800Z_2399001`. e43 has 10/320 directed contact entries positive and 10 observed distance entries. Its frozen evaluation `unified4d_v8distance_eval_train30_45_val46_test43_20260918T134900Z_2400000` reports contact precision/recall/F1 `1.0/1.0/1.0`, task-relevant F1 `1.0`, and masked MuJoCo reported-pair distance MAE `0.0023883 m`; RGB is not improved (`0.0437417 -> 0.0446885`) while temporal MAE changes `0.0263354 -> 0.0260855`.
- Integrity limitation: e45's v8 replay had previously been recorded as `sanity_only` and excluded from train/validation/test. Although the later gate named e45 as train, this conflicts with that earlier exclusion. Accordingly, this run is retained only as an integrity-limited engineering diagnostic, not a valid heldout method result. Even without that conflict, MuJoCo `c.dist` is finite only for reported contact pairs; it is not an SDF, mesh-penetration label, or evidence that repaired RGB visually fixes interpenetration. It makes no WAM-improvement, action-ranking, task-success, pi0.5, or VLA-benefit claim.

## 2026-09-18 v8 masked-distance unified fresh-heldout gate (non-physical, distance-uninformative)

- `../V8_SIGNED_DISTANCE_UNIFIED_FRESH_GATE_20260918.md` locked train `{30,45}`, validation `{48}`, fresh heldout `{47}` before the e47 v8 D0 was collected. All items use the explicit observed-pair mask/NaN distance schema. No repeated-current-frame baseline was included.
- e30 train v8 collection `d0v8_e30_train_20260918T123200Z_2381000` completed with zero qpos/qvel reset error and three contact frames. e48 validation `d0v8_e48_val_20260918T123600Z_2382000` completed with zero reset error and zero contact frames. Both pair/WAM/target-only geometry chains completed using input-only FastWAM. Their v8 bridges are `geometry_d0v8_e30_train_20260918T125100Z_2386000` and `geometry_d0v8_e48_val_20260918T125100Z_2386001`.
- Initial selection `unified4d_v8distance_selection_train30_45_val48_20260918T125300Z_2387000` failed before optimization/checkpoint because the older geometry loader required the exact v7 field set. The only recovery change permits exactly the two named v8 target fields, retaining all SHA/timing/target-only checks. Recovery selection `unified4d_v8distance_selection_train30_45_val48_recovery_20260918T125700Z_2387100` selected step 300. e48 has zero observed-distance pairs, so its validation signed-distance loss is safely zero with count 0; it cannot validate distance quality or select a distance-optimal checkpoint.
- Only then was fresh e47 opened: `d0v8_e47_freshheldout_20260918T130100Z_2388000` had zero reset error but zero contact frames; its input-only WAM and v8 bridge completed. Frozen `unified4d_v8distance_eval_train30_45_val48_test47_20260918T131000Z_2391001` reports RGB raw `0.0508505 -> 0.0443496`, temporal `0.0325599 -> 0.0304248`, depth log-MAE `0.823763`, position `0.014280`, rotation-6D `0.017240`. It has 0/320 contact positives and zero observed-distance entries: contact F1 is not interpretable and masked-distance MAE is `null` rather than fabricated.
- This is one episode-isolated **prediction/plumbing diagnostic only**. It provides no evidence for contact restoration, penetration correction, signed-distance generalization, visual physical consistency, action ranking, task success, or pi0.5/VLA benefit. Personal port10129 π0.5 server PID 2384756 was verified then stopped; GPU preflight/release checks passed.
